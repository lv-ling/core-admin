# AGENTS.md - 开发指南

本文件为开发指南**总览**。细分规则已拆到 **`.cursor/rules/agents/`** 下子规则，Agent 与开发时按场景查阅对应子规则。

---

## 📋 项目信息

- **项目名称**: 管理系统
- **技术栈**: Vue 3 + TypeScript + Vite + Pinia + Vue Router
- **UI 组件库**: Element Plus（优先使用）
- **Node 版本**: ^20.19.0 || >=22.12.0

---

## 📑 子规则索引

| 子规则                         | 说明                                                                |
| ------------------------------ | ------------------------------------------------------------------- |
| **agents/index.mdc**           | 开发指南入口，子规则索引与「页面开发与规则引用」                    |
| **agents/language.mdc**        | 语言规则（对话、注释、提交信息、命名语言）                          |
| **agents/commit.mdc**          | 提交信息规范（type + 中文描述）                                     |
| **agents/code.mdc**            | 代码规则（UI、样式、TS、命名、质量）                                |
| **agents/directory.mdc**       | 目录规范（src 结构、各目录用途）                                    |
| **agents/eslint-prettier.mdc** | ESLint 与 Prettier                                                  |
| **agents/development.mdc**     | 开发规范（API、Pinia、路由、组件等）                                |
| **agents/sfc-structure.mdc**   | 单文件组件结构（script 在顶层 → template → style，块内顺序 1 ～ 7） |
| **agents/router.mdc**          | 路由规范（懒加载、模块、meta）                                      |
| **agents/naming.mdc**          | 命名约定（常量、变量、接口、文件）                                  |
| **agents/typescript.mdc**      | TypeScript 与类型（路径别名、接口、API 类型）                       |
| **agents/commands.mdc**        | 核心命令（dev、build、lint、format、type-check）                    |
| **agents/error-handling.mdc**  | 错误处理（try/catch、ElMessage、校验）                              |
| **agents/reminder.mdc**        | 重要提醒（必守项汇总）                                              |

---

## 🚀 使用方式

- **Agent**：优先查阅 **`.cursor/rules/index.mdc`**，再按需引用 `@agents/xxx.mdc`。
- **列表页**（带搜索表单 + 表格 + 分页）：在 `src/views/[模块]/` 下含 `index.vue`、`columns.tsx`、`form-items.tsx`。
- 保证与上述规则一致，减少重复说明与 token 消耗。
